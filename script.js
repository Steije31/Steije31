function highlightShape(shape) {
    // Your existing code...
    shape.classList.add('detected'); // Add detected class
}

function clearHighlights() {
    // Your existing code...
    shape.classList.remove('detected'); // Remove detected class
}